#https://leetcode.com/problems/majority-element/submissions/1554642964/?envType=study-plan-v2&envId=top-interview-150

class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        nums_freq = {}
        for num in nums:
            if num in nums_freq:
                nums_freq[num] += 1
            else:
                nums_freq[num] = 1
        majority = 0
        majority_num = None
        for num , cnt in nums_freq.items():
            if cnt > majority:
                majority = cnt
                majority_num = num

        return majority_num
        
